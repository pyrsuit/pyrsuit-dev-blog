const e='<div class="flex items-baseline justify-between mb-2">\n  <h1 class="text-2xl font-light tracking-tight">A training cycle planning agent</h1>\n  <time class="text-sm text-gray-600 ml-4">July 5, 2026</time>\n</div>\n\n> *An agent that turns office days, a workout split, and menstrual cycle phase into a scheduled week - exportable straight to a calendar.*\n\n## How it works\n\n```mermaid\ngraph LR\n    A[CLI prompts] --> B[WeekInput]\n    B --> C[Pydantic AI Agent]\n    C --> D[WeekPlan]\n    D --> E[CLI output]\n    D --> F[.ics export]\n```\n\nYou answer a few prompts - office days, long office days, how many strength/climbing/running/yoga sessions you want, rest days, cycle phase, and any notes - and the agent returns a day-by-day plan with intensity levels and a load summary, optionally exported to your calendar.\n\n## The components\n\n**`WeekInput`** bundles the week\'s constraints: `office_days`, `long_days` (workdays that run until 7pm), a `WorkoutSplit` across strength, climbing, running, yoga and "random", the current `CyclePhase`, and free-text notes.\n\n**The system prompt** (`prompts.py`) is where the actual planning logic lives, not in code. It encodes training load guidance per cycle phase - easing off in the menstrual phase, going hard at ovulation, tapering through the luteal phase - commute and time constraints on office days, and placement rules: never two high-intensity days back to back, keep strength and climbing at least a day apart, put climbing before home-office days to protect recovery.\n\n**`WeekPlan`**, the structured output, is enforced with Pydantic AI\'s [`NativeOutput`](https://ai.pydantic.dev/output/) - seven `ScheduledActivity` entries (activity, intensity, optional start time, notes) plus a summary and an overall load assessment.\n\n**`gemma4:e2b`** runs locally through `Ollama`, the same setup as the [meal planner](https://pyrsuit.dev/lab/local_shopping_agent) agent - `OllamaProvider` pointed at the OpenAI-compatible `/v1` endpoint.\n\n**`calendar_sync.py`** exports the plan to `.ics` via the `icalendar` package, so the week goes straight from terminal to calendar app.\n\nRepo and setup are in the [README](https://github.com/pyrsuit/week-planner-agent).\n';export{e as default};
